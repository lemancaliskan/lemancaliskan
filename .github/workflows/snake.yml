name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write      

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: false   

      - name: Generate snake files
        uses: Platane/snk@v3
        with:
          github_user_name: LemanCaliskan
          outputs: |
            dist/github-snake.svg?palette=github-light&color_snake=#61DAFB
            dist/github-snake-dark.svg?palette=github-dark&color_snake=#61DAFB

      - name: Deploy to output branch
        uses: peaceiris/actions-gh-pages@v3    
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: output
          publish_dir: dist
          force_orphan: true
